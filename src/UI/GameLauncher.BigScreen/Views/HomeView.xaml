<Page x:Class="GameLauncher.BigScreen.Views.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="1080" d:DesignWidth="1920"
      Background="Black"
      Title="Home">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a1a2e" Offset="0"/>
                <GradientStop Color="#16213e" Offset="0.5"/>
                <GradientStop Color="#0f3460" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="60">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,40,0,80">
                <TextBlock Text="G A M E L A U N C H E R"
                           FontSize="52"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#00d4ff" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
                    </TextBlock.Effect>
                </TextBlock>
               <TextBlock Text="B i g S c r e e n"
                           FontSize="28"
                           Foreground="#adb5bd"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>

            <!-- Cards -->
            <Grid Grid.Row="1" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Card 1: GameLauncher -->
                <Border x:Name="Card1"
                        Grid.Column="0"
                        CornerRadius="20"
                        Padding="20"
                        Background="#111122"
                        BorderThickness="3"
                        BorderBrush="Transparent"
                        Cursor="Hand">
                    <!-- ... Styles ... -->
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedIndex}" Value="0">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <GradientStop Color="#00d4ff" Offset="0"/>
                                                <GradientStop Color="#0096ff" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#00d4ff" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <Viewbox Stretch="Uniform">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="🎮" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock Text="BIBLIOTECA" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center"/>
                            <TextBlock Text="Tus juegos locales" FontSize="14" Foreground="#ced4da" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Viewbox>
                </Border>

                <!-- Card 2: PiviGames -->
                <Border x:Name="Card2"
                        Grid.Column="2"
                        CornerRadius="20"
                        Padding="20"
                        Background="#111122"
                        BorderThickness="3"
                        BorderBrush="Transparent"
                        Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedIndex}" Value="1">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <GradientStop Color="#00d4ff" Offset="0"/>
                                                <GradientStop Color="#0096ff" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#00d4ff" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <Viewbox Stretch="Uniform">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="🕷️" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock Text="PIVIGAMES" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="Descargas Pivi" FontSize="14" Foreground="#ced4da" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Viewbox>
                </Border>

                <!-- Card 3: BlizzBoyGames -->
                <Border x:Name="Card3"
                        Grid.Column="4"
                        CornerRadius="20"
                        Padding="20"
                        Background="#111122"
                        BorderThickness="3"
                        BorderBrush="Transparent"
                        Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedIndex}" Value="2">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <GradientStop Color="#00d4ff" Offset="0"/>
                                                <GradientStop Color="#0096ff" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#00d4ff" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <Viewbox Stretch="Uniform">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="❄️" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock Text="BLIZZBOY" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="Descargas Blizz" FontSize="14" Foreground="#ced4da" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Viewbox>
                </Border>

                <!-- Card 4: System Info -->
                <Border x:Name="Card4"
                        Grid.Column="6"
                        CornerRadius="20"
                        Padding="20"
                        Background="#111122"
                        BorderThickness="3"
                        BorderBrush="Transparent"
                        Cursor="Hand">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedIndex}" Value="3">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <GradientStop Color="#00d4ff" Offset="0"/>
                                                <GradientStop Color="#0096ff" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#00d4ff" BlurRadius="40" ShadowDepth="0" Opacity="0.8"/>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <Viewbox Stretch="Uniform">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="🖥️" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock Text="SISTEMA" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                            <TextBlock Text="Info y Escaneo" FontSize="14" Foreground="#ced4da" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Viewbox>
                </Border>
            </Grid>

            <!-- Footer Instructions -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,60,0,20">
                <TextBlock Text="[D-Pad]" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="#00d4ff"
                           Margin="0,0,10,0"/>
                <TextBlock Text="Seleccionar" 
                           FontSize="24" 
                           Foreground="#ced4da"
                           Margin="0,0,40,0"/>
                <TextBlock Text="[B]" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="#00d4ff"
                           Margin="0,0,10,0"/>
                <TextBlock Text="Entrar" 
                           FontSize="24" 
                           Foreground="#ced4da"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
